class ReconModule:
    def enumerate_subdomains(self, domain):
        print(f"[+] Enumerating subdomains for {domain}")
        # Dummy logic
        subdomains = ["admin.example.com", "test.example.com"]
        return subdomains

    def brute_force_directories(self, url, wordlist):
        print(f"[+] Brute-forcing directories on {url} with {wordlist}")
        # Dummy logic
        directories = ["/admin", "/login", "/config"]
        return directories